# DISABLED - Using deploy.yml for direct GitHub Pages deployment
# Jekyll was interfering with XML sitemap serving
name: Deploy Jekyll (DISABLED)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This workflow is disabled. Use deploy.yml instead.'
